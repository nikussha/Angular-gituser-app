import { StyledirDirective } from './styledir.directive';

describe('StyledirDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledirDirective();
    expect(directive).toBeTruthy();
  });
});
